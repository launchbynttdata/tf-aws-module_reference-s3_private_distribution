name: Dependabot Auto-Merge

on:
  pull_request:
    types: [ opened, reopened, synchronize ]

jobs:
  dependabot-automerge:
    name: Dependabot Auto-Merge
    permissions:
      contents: write
      pull-requests: write
    uses: launchbynttdata/launch-workflows/.github/workflows/reusable-pr-dependabot-automerge.yml@0.15.0
    secrets: inherit # pragma: allowlist secret
